<%= form_with(model: dock) do |form| %>
  <% if dock.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dock.errors.count, "error") %> prohibited this dock from being saved:</h2>

      <ul>
        <% dock.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <% if current_user %>

  <div>
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div>
    <%= form.file_field :files, multiple: true, name: 'dock[files][]'%>
  </div>
</br>
</br>
  <div>
    <%= form.submit %>
  </div>
  <% end %>
<% end %>
